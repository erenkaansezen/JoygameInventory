@model IEnumerable<Licence>
@{
    Layout = "_Layout";
}

<!DOCTYPE html>
<html lang="tr">
<head>

    <style>
        .table-container {
            margin-top: 20px;
        }

        .table th, .table td {
            vertical-align: middle;
        }

        .btn-custom {
            padding: 8px 12px;
            font-size: 14px;
        }

        .btn-edit {
            background-color: #f0ad4e;
            color: white;
        }

        .btn-delete {
            background-color: #d9534f;
            color: white;
        }

        .btn-view {
            background-color: #5bc0de;
            color: white;
        }
    </style>
</head>

<!-- Sayfa Başlığı -->
<div class="container">
    <h2 class="my-4 text-center">
         Lisans Listesi        
    </h2>
    <hr />
    <div class="row">
        <div class="col-md-6">
            <a asp-action="LicenceCreate" class="btn btn-outline-success btn-custom ms-2 w-100">Lisans Ekle</a>
        </div>
        <div class="col-md-6">
            <form asp-action="LicenceList" method="get">
                <div class="input-group mb-3">
                    <input type="text" name="searchTerm" class="form-control" placeholder="Lisans İsmi ile ara" />
                    <button class="btn btn-outline-secondary" type="submit">Ara</button>
                </div>
            </form>
        </div>
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success">
                @TempData["SuccessMessage"]
            </div>
        }

        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger">
                @TempData["ErrorMessage"]
            </div>
        }

        <!-- Kullanıcılar Tablosu -->
        <div class="table-container">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th></th>
                        <th>Lisans Adı</th>
                        <th>Bitiş Süresi</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var lisans in Model)
                    {
                        <tr>
                            <td>@lisans.Id</td>
                            <td>@lisans.LicenceName</td>
                            <td>@lisans.LicenceEndDate.ToString("dd-MM-yyyy")</td>
                            <td class="text-end">
                                <!-- Align buttons to the right -->
                                <a asp-action="LicenceDetails" asp-route-id="@lisans.Id" class="btn btn-outline-dark btn-custom">Lisans Detayı</a>

                                <form asp-controller="Management" asp-action="StaffDelete" method="post" onsubmit="return confirm('emin misiniz')" style="display: inline;">
                                    <input type="hidden" value="@lisans.Id" name="id">
                                    <button type="submit" class="btn btn-danger btn-custom">Lisansı Sil</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>


    <!-- Bootstrap ve jQuery JS -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</div>
</html>
